# {{ ansible_managed }}

api.port: 1323
etcd.address: "{{ serverledge_etcd_addr }}"
registry.area: "{{ serverledge_region }}"
registry.nearby.interval: 5
registry.monitoring.interval: 10

cloud: yes
offloading: yes
#scheduler.policy: edgeonly

container.pool.memory: {{ ansible_memtotal_mb - 1024 }}
container.pool.cpus: {{ ansible_processor_nproc }}
