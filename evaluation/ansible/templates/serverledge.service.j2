# {{ ansible_managed }}

[Unit]
Description=Serverledge Node
StartLimitIntervalSec=0

[Service]
Type=exec
ExecStart={{ serverledge_bindir }}/{{ bin }} {{ serverledge_config }}
ExecStop=/bin/kill -TERM $MAINPID
Nice=-10
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target